# このファイルは **テンプレート** です。
# 実値を入れたファイルは `.env` (このディレクトリ直下) に配置してください。
# compose の variable substitution が自動で読みます。
#
# .env は .gitignore で除外されます。
#
# 並列起動時に host port が衝突しないよう、project ごとに数値を変えてください。

HOST_PORT=8080
